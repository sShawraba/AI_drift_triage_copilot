You are a drift triage specialist. Assess the following drift report and decide the severity and next step.

Drift event:
- Model version: {model_version}
- PSI (numeric): {psi_numeric}
- Chi2 (categorical): {chi2_categorical}
- Output distribution drift: {output_drift}
- Reported severity: {severity}

Your response must be a JSON object with exactly these keys:
- "triage_result": a one-sentence summary of the assessment.
- "next_step": either "action" (if the drift is medium or high, or if action is needed) or "comms" (if low, no action needed).
- "explanation": a brief explanation of your decision.

Return ONLY the JSON, no other text.