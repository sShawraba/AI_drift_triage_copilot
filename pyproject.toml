[project]
name = "ai-drift-triage-copilot"
version = "0.1.0"
description = "AI Drift Triage Copilot"
requires-python = ">=3.9"
dependencies = [
    "mlflow",
    "scikit-learn",
    "pandas",
    "pydantic",
    "sqlalchemy",
]
